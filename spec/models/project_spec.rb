require 'spec_helper'

describe Project do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Project.create!(@valid_attributes)
  end
  
    it "should be set to either public or private"
  
  describe "Given valid attributes" do
    it "should have a user with admin privileges in its memberships"
    it "should have a valid name"
    it "should have a valid description"
  end
end
